version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install --legacy-peer-deps 
  build:
    commands:
      - npx prisma generate  
      - npx prisma migrate deploy 
      - npm run build 
artifacts:
  files:
    - '**/*'
